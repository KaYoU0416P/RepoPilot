# sample_repo

Target repository used by RepoPilot's demo and tests.

`calculator.divide` has no zero check, so `test_divide_by_zero_raises_value_error`
fails. That failing test is the task the agent is asked to fix.
